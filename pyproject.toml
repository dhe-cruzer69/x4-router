[project]
name = "x4-router"
version = "0.0.1"
description = "X4 router — routing, policies, registry"
requires-python = ">=3.11"
readme = "README.md"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
